[type="button"]:not([class]),
[type="reset"]:not([class]),
[type="submit"]:not([class]),
[role="button"]:not([class]),
button:not([class]),
.button {
  background-color: var(--primary-transparent);
  border: 1px solid var(--primary);
  border-radius: var(--border-radius);
  color: var(--primary);
  display: inline-block;
  font-size: inherit;
  line-height: 1;
  overflow: visible;
  padding: 1rem 2.5rem;
  text-align: center;
  text-decoration: none;
  text-transform: none;
  transform: scale(1);
  transition: opacity 0.2s ease;
}

[type="button"]:not([class]),
[type="reset"]:not([class]),
[type="submit"]:not([class]),
button:not([class]),
.button {
  -webkit-appearance: none;
}

button:not([class]) + button:not([class]) {
  margin: auto 0.5rem;
}

[type="button"]:not([class]):active,
[type="reset"]:not([class]):active,
[type="submit"]:not([class]):active,
button:not([class]):active,
.button:active {
  transform: scale(0.98);
}

[type="button"]:not([class]):not([disabled]):hover,
[type="reset"]:not([class]):not([disabled]):hover,
[type="submit"]:not([class]):not([disabled]):hover,
[role="button"]:not([class]):not([disabled]):hover,
button:not([class]):not([disabled]):hover,
.button:not([disabled]):hover,
[type="button"]:not([class]):not([disabled]):focus,
[type="reset"]:not([class]):not([disabled]):focus,
[type="submit"]:not([class]):not([disabled]):focus,
[role="button"]:not([class]):not([disabled]):focus,
button:not([class]):not([disabled]):focus,
.button:not([disabled]):focus {
  background-color: var(--primary);
  border: 1px solid var(--primary);
  box-shadow: 0 0 10px var(--primary);
  color: var(--primary-bg);
  cursor: not-allowed;
}


