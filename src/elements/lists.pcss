ul,
ol {
  margin: var(--line-height) 0;
  padding-left: 2rem;
}
ul ul,
ol ol {
  margin: 0;
}

li {
  font-size: 1rem;
  line-height: var(--line-height);
  max-width: 80ch;
  max-width: calc(80ch - 3rem);
}
